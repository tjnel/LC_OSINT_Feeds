dist: xenial   # required for Python >= 3.7
language: python
python:
  - "3.6"
install:
  - pip install -r requirements.txt
script: python3 /src/generate_urlhaus_list.py || python3 /src/generate_bambenek_list.py